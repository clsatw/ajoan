import { AddAttrDirective } from './add-attr.directive';

describe('AddAttrDirective', () => {
  it('should create an instance', () => {
    const directive = new AddAttrDirective();
    expect(directive).toBeTruthy();
  });
});
